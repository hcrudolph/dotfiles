#!/bin/bash

# General
alias ..='cd ..'
alias ll='ls -l'
alias la='ls -la'
alias x='exit'

# Git
alias init='git init'
alias pull='git pull'
alias push='git push'
alias add='git add'
alias status='git status'
alias commit='git commit -m'
